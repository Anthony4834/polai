{
  "name": "polai",
  "version": "1.0.0",
  "description": "Analyze United States political bias and suggest neutral wording.",
  "private": true,
  "scripts": {
    "install:all": "npm ci --prefix client && npm ci --prefix server",
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "build": "npm run build --prefix client && npm run build --prefix server",
    "lint": "npm run lint --prefix client",
    "test": "npm run test --prefix client && npm run test --prefix server",
    "check": "npm run lint && npm run test && npm run build"
  },
  "keywords": [],
  "author": "Anthony Griffin",
  "license": "ISC"
}
